# TODO

- [ ] Fix project card image cropping so full display picture fits
  - [ ] Update `components/ProjectsPinnedScroll.tsx` hero image to use `object-contain` instead of `object-cover`
  - [ ] Update `components/ProjectsCardDeck.tsx` hero image to use `object-contain` instead of `object-cover`
  - [ ] Update `app/projects/page.tsx` project grid thumbnails to use `object-contain` (or adjust height) so the whole image is visible
  - [ ] Update `components/ProjectGallery.tsx` screenshot viewport already uses `object-contain`; verify no further changes needed

